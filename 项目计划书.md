# 10×10 飞机对战游戏 — 架构计划书

---

## 一、项目文件全景图

```
小组/                              ← 项目根目录
│
├── core/
│   └── game_logic.py              ← 核心游戏逻辑（底层模块）
│
├── static/
│   ├── index.html                 ← 页面结构
│   ├── style.css                  ← 样式表
│   └── main.js                    ← 前端交互逻辑
│
├── local_game.py                  ← 本地双人对战服务
├── server.py                      ← WebSocket联机对战服务
│
├── README.md                      ← 项目说明
├── docs/                          ← 文档
    ├── 使用手册.md

```

### 文件依赖关系

```
                    core/game_logic.py  
                    ┌─────────────────┐
                    │ GameLogic 类    │
                    │ - place_planes()│
                    │ - attack()      │
                    │ - is_defeated() │
                    │ - get_board()   │  


                             │ from core.game_logic import GameLogic
              ┌──────────────┴──────────────┐
              ▼                             ▼
    local_game.py                       server.py
    ┌──────────────────┐          ┌──────────────────────┐
    │ LocalGame 类     │          │ PlaneBattleServer 类 │
    │ - 回合控制        │          │ - WebSocket 服务     │
    │ - HTTP 通信       │          │ - 房间管理           │
    │ - 调用逻辑     │          │ - 调用逻辑        │
    └────────┬─────────┘          └───────────┬──────────┘
             │ HTTP JSON                      │ WebSocket JSON
             └──────────┬─────────────────────┘
                        │
                        ▼
              static/main.js (B)
              ┌──────────────────────┐
              │ - 调用 D(本地模式)    │
              │ - 调用 E(联机模式)    │
              │ - 渲染 static/index  │
              └──────────────────────┘
```


---

## 二、 核心游戏逻辑

### 负责文件：`core/game_logic.py`

> **定位**：整个项目的底层基石。只做规则计算，不碰界面、网络、回合。  
> **被依赖方**：100%依赖此文件，必须导入使用。

### 2.1 内部数据结构

```python
# 棋盘常量
BOARD_SIZE = 10          # 10×10 棋盘
PLANE_COUNT = 3          # 每方3架飞机

# 格子状态码（棋盘二维数组取值）
EMPTY   = 0   # 空格
BODY    = 1   # 飞机机身/机翼
HEAD    = 2   # 飞机机头

# 攻击结果码（attack() 返回值）
MISS    = 0   # 未命中
HIT     = 1   # 普通击中（机身/机翼）
HEADSHOT = 2  # 命中机头 → 该飞机直接击毁
```

### 2.2 类与方法清单

```python
class GameLogic:
    """
    飞机对战游戏核心逻辑
    职责：棋盘初始化 / 飞机随机摆放 / 攻击判定 / 胜负判断
    禁止：操作界面、处理网络、控制回合
    """

    # ========== 属性 ==========
    BOARD_SIZE: int          # 10
    PLANE_COUNT: int         # 3
    board_p0: list[list[int]]  # 玩家0的棋盘 (10×10)
    board_p1: list[list[int]]  # 玩家1的棋盘 (10×10)
    attacked_p0: set           # 玩家0已受攻击坐标 (防重复)
    attacked_p1: set           # 玩家1已受攻击坐标
    plane_heads_p0: list       # 玩家0所有机头坐标 [(r,c),...]
    plane_heads_p1: list       # 玩家1所有机头坐标

    # ========== 方法（共5个公开方法）==========

    def __init__(self) -> None:
        """
        初始化两个10×10空棋盘（全0）
        初始化已攻击坐标集合为空
        调用自动摆放飞机
        """

    def _generate_plane_offsets(self, direction: int) -> list[tuple]:
        """
        【内部方法】生成飞机各部件相对机头的偏移量
        参数 direction: 0=上, 1=下, 2=左, 3=右
        返回: 5个偏移坐标列表 [(dr,dc),...]（含机头自身）
        说明: 1513规则 — 1机头+5排长+1行机身+3格翼宽
        """

    def _can_place_plane(self, board, head_r, head_c, offsets) -> bool:
        """
        【内部方法】检查飞机能否放在指定位置
        校验: 不出界(0-9) / 不重叠(目标格子全为0)
        返回: True=可放置, False=不可
        """

    def _place_one_plane(self, board, head_r, head_c, offsets) -> None:
        """
        【内部方法】将一架飞机写入棋盘
        机头写2, 其余部件写1
        同时记录机头坐标到 plane_heads 列表
        """

    def place_planes(self, player_id: int) -> list[list[int]]:
        """
        【公开】为指定玩家随机摆放 PLANE_COUNT 架飞机
        参数:
            player_id: 0 或 1
        返回:
            10×10 二维数组，0=空格, 1=机身, 2=机头
        算法:
            1. 随机选方向(0-3)
            2. 随机选机头坐标
            3. 用 _can_place_plane 校验
            4. 用 _place_one_plane 写入
            5. 循环直到 PLANE_COUNT 架全放好
        """

    def attack(self, player_id: int, row: int, col: int) -> int:
        """
        【公开】对指定玩家棋盘发起一次攻击
        参数:
            player_id: 被攻击的玩家 0 或 1
            row, col:  攻击坐标
        返回:
            MISS(0): 该格为空格
            HIT(1):  该格为机身/机翼
            HEADSHOT(2): 该格为机头 → 将该飞机所有格子标记为已击毁
        抛异常:
            ValueError: 坐标重复攻击
            IndexError: 坐标越界
        处理:
            1. 检查坐标是否在 attacked 集合中 → 抛 ValueError
            2. 将坐标加入 attacked 集合
            3. 读取棋盘值，返回对应结果码
            4. 若为机头(2)，找到该飞机所有格子标记为已发现
        """

    def is_player_defeated(self, player_id: int) -> bool:
        """
        【公开】判断指定玩家是否所有飞机被击毁
        逻辑: 遍历该玩家所有机头坐标，若全部被攻击过 → 返回 True
        返回: True=全部击毁, False=仍有存活
        """

    def get_board_state(self, player_id: int) -> list[list[int]]:
        """
        【公开】获取某玩家棋盘当前状态
        返回: 10×10 二维数组
        注意: 只返回棋盘原始布局，不暴露未攻击坐标
        """
```

### 2.3 自测要求

```python
if __name__ == "__main__":
    # 测试1: 棋盘初始化 → 验证两个棋盘大小10×10
    # 测试2: 飞机摆放 → 验证每方恰好3个机头(值为2)
    # 测试3: 飞机不出界 → 验证所有飞机格子坐标在0-9范围
    # 测试4: 飞机不重叠 → 验证所有飞机格子不重叠
    # 测试5: attack(未命中) → 验证返回0
    # 测试6: attack(命中机身) → 验证返回1
    # 测试7: attack(命中机头) → 验证返回2
    # 测试8: 重复攻击 → 验证抛 ValueError
    # 测试9: is_player_defeated → 击毁所有机头后返回True
    # 测试10: get_board_state → 验证返回正确尺寸
```

---

## 三、前端页面

### 负责文件：`static/index.html`、`static/style.css`、`static/main.js`

> **定位**：用户直接交互的界面。双棋盘渲染、点击攻击、结果展示。  
> **对接**：同时对接本地模式和联机模式

### 3.1 `static/index.html` — 页面结构

```html
<!-- 需要实现的DOM结构 -->
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>飞机对战游戏</title>
    <link rel="stylesheet" href="style.css">
</head>
<body>
    <!-- 顶部标题栏 -->
    <div id="header">
        <h1>飞机对战游戏</h1>
        <div id="mode-selector">
            <!-- 模式切换：本地对战 / 联机对战 -->
            <button id="btn-mode-local">本地双人对战</button>
            <button id="btn-mode-online">联机对战</button>
        </div>
    </div>

    <!-- 联机房间区（联机模式显示） -->
    <div id="room-panel" style="display:none">
        <input id="input-room-id" placeholder="房间号">
        <button id="btn-create-room">创建房间</button>
        <button id="btn-join-room">加入房间</button>
        <span id="room-status"></span>
    </div>

    <!-- 主游戏区：双棋盘并排 -->
    <div id="game-area">
        <!-- 我方棋盘 -->
        <div class="board-container">
            <h2>我方棋盘（你的飞机）</h2>
            <div id="board-my" class="board"></div>
        </div>

        <!-- 提示信息区 -->
        <div id="info-panel">
            <p id="turn-indicator">当前回合：玩家1</p>
            <p id="message-box"></p>
            <p id="score-board">剩余飞机 — 我方:3 | 敌方:3</p>
            <button id="btn-restart">重新开始</button>
        </div>

        <!-- 敌方棋盘 -->
        <div class="board-container">
            <h2>敌方棋盘（攻击目标）</h2>
            <div id="board-enemy" class="board"></div>
        </div>
    </div>

    <!-- 胜负弹窗（默认隐藏） -->
    <div id="game-over-modal" style="display:none">
        <div class="modal-content">
            <h2 id="winner-text"></h2>
            <button id="btn-new-game">再来一局</button>
        </div>
    </div>

    <script src="main.js"></script>
</body>
</html>
```

> **要实现的具体元素清单：**
> - `#board-my` — 我方10×10棋盘容器 (100个格子，由JS动态生成)
> - `#board-enemy` — 敌方10×10棋盘容器 (100个格子，可点击攻击)
> - `#turn-indicator` — 当前回合文字
> - `#message-box` — 攻击结果提示文字
> - `#score-board` — 双方剩余飞机数
> - `#btn-restart` — 重新开始按钮
> - `#game-over-modal` — 游戏结束弹窗
> - `#mode-selector` — 本地/联机模式切换
> - `#room-panel` — 联机房间操作面板

### 3.2 `static/style.css` — 样式表

```css
/* ========== 需要实现的样式类 ========== */

/* 1. 页面整体布局 */
body { /* 居中布局、背景色、字体 */ }
#game-area { /* flex横向排列：我方棋盘 + 信息区 + 敌方棋盘 */ }

/* 2. 棋盘网格 .board */
.board { /* 10×10 CSS Grid布局 */ }

/* 3. 单个格子 .cell (10×10=100个，JS动态生成时附加) */
.cell {
    /* 默认状态：浅蓝色/白色背景，带边框 */
    width: 40px; height: 40px;
    border: 1px solid #333;
    cursor: pointer;          /* 仅敌方棋盘可点击 */
}

/* 4. 格子状态配色（通过附加类切换） */
.cell-normal     { background: #e3f2fd; }  /* 未攻击 */
.cell-miss       { background: #bdbdbd; }  /* 未命中 (结果0) */
.cell-hit        { background: #ef5350; }  /* 命中机身 (结果1) */
.cell-headshot   { background: #b71c1c; }  /* 命中机头 (结果2) → 深红+动画 */
.cell-destroyed  { background: #ff8a65; }  /* 机头被爆→整架飞机标红 */

/* 5. 我方棋盘特殊样式 */
#board-my .cell { cursor: default; }        /* 我方棋盘不可点击 */

/* 6. 信息面板 */
#info-panel { /* 居中、边距、字体 */ }

/* 7. 胜负弹窗 */
#game-over-modal { /* 全屏遮罩、居中弹窗 */ }

/* 8. 响应式（加分项） */
@media (max-width: 768px) { /* 移动端适配 */ }
```

> **要实现的具体CSS规则：**
> - 5种格子状态类（`.cell-normal` / `.cell-miss` / `.cell-hit` / `.cell-headshot` / `.cell-destroyed`）
> - 10×10 Grid布局的 `.board`
> - 全屏弹窗 `#game-over-modal`
> - 联机面板 `#room-panel`
> - 我方棋盘格子 `cursor: default`（不可点击）

### 3.3 `static/main.js` — 前端交互逻辑

```javascript
// ========== 需要实现的全部函数 ==========

// ===== 1. 棋盘生成 =====
function createBoard(boardElement, isMyBoard) {
    // 参数: boardElement — DOM容器; isMyBoard — 是否我方棋盘
    // 逻辑: 双重循环生成 10×10=100 个 div.cell
    //       每个格子记录 data-row, data-col 属性
    //       我方棋盘格子不绑定点击事件
    //       敌方棋盘格子绑定 click → onCellClick()
}

// ===== 2. 格子点击处理 =====
function onCellClick(event) {
    // 1. 读取 event.target 的 data-row, data-col
    // 2. 检查是否已攻击过 → 拦截重复点击
    // 3. 检查是否轮到自己回合 → 不是则提示
    // 4. 检查游戏是否已结束
    // 5. 根据当前模式调用不同发送函数:
    //    本地模式 → sendAttackToLocal(row, col)
    //    联机模式 → sendAttackToServer(row, col)
}

// ===== 3. 发送攻击（本地模式）=====
function sendAttackToLocal(row, col) {
    // 方式: fetch POST 到 http://localhost:5000/attack
    // 请求体: JSON.stringify({ player: currentPlayer, row, col })
    // 接收响应后调用 handleAttackResult(data)
}

// ===== 4. 发送攻击（联机模式）=====
function sendAttackToServer(row, col) {
    // 方式: ws.send(JSON.stringify({ type: "attack", room_id, row, col }))
    // ws 为全局 WebSocket 连接对象
}

// ===== 5. 攻击结果处理 =====
function handleAttackResult(data) {
    // data 结构: { result: 0|1|2, message, current_turn, game_over, winner }
    // 1. 更新敌方棋盘对应格子的样式类
    // 2. 更新 #message-box 文字提示
    // 3. 更新 #turn-indicator 回合显示
    // 4. 更新 #score-board 剩余飞机数
    // 5. 若 game_over → 调用 showGameOver(winner)
}

// ===== 6. 格子样式更新 =====
function updateCellStyle(boardId, row, col, resultCode) {
    // resultCode: 0 → add 'cell-miss'
    //            1 → add 'cell-hit'
    //            2 → add 'cell-headshot'
    // 同时移除 cell-normal，禁止再次点击
}

// ===== 7. 游戏结束弹窗 =====
function showGameOver(winner) {
    // 1. 显示 #game-over-modal
    // 2. 设置 #winner-text 内容 = "玩家X获胜！"
    // 3. 冻结全部棋盘点击
}

// ===== 8. 重新开始 =====
function restartGame() {
    // 1. 隐藏弹窗
    // 2. 清空所有格子样式
    // 3. 重置回合标识
    // 4. 重新请求服务端初始化
}

// ===== 9. WebSocket 连接管理（联机模式）=====
let ws = null;

function connectWebSocket() {
    // ws = new WebSocket("ws://localhost:8765");
    // 绑定 onopen / onmessage / onclose / onerror
}

function handleServerMessage(event) {
    // 解析 event.data (JSON)
    // 按 type 字段分发:
    //   "room_created" → 显示房间号
    //   "room_joined"  → 更新房间状态
    //   "attack_result" → handleAttackResult(data)
    //   "game_start"   → 初始化棋盘
    //   "opponent_disconnected" → 提示对方掉线
}

// ===== 10. 模式切换 =====
function switchMode(mode) {
    // mode: "local" | "online"
    // 本地: 隐藏房间面板, 调用 D 的HTTP接口
    // 联机: 显示房间面板, 建立WebSocket连接
}

// ===== 11. 初始化入口 =====
window.onload = function() {
    // 1. createBoard(boardMy, true)    生成我方棋盘
    // 2. createBoard(boardEnemy, false) 生成敌方棋盘
    // 3. 默认本地模式
    // 4. 绑定按钮事件
};
```

> ** 前端网页要实现的具体函数清单（共11个）：**
> 1. `createBoard()` — 棋盘DOM生成
> 2. `onCellClick()` — 点击事件分发
> 3. `sendAttackToLocal()` — 本地HTTP通信
> 4. `sendAttackToServer()` — 联机WebSocket通信
> 5. `handleAttackResult()` — 结果处理+界面更新
> 6. `updateCellStyle()` — 格子样式切换
> 7. `showGameOver()` — 弹窗
> 8. `restartGame()` — 重置
> 9. `connectWebSocket()` — WS连接
> 10. `handleServerMessage()` — WS消息分发
> 11. `switchMode()` — 模式切换
> 12. `window.onload` — 入口初始化

---

## 四、本地双人对战服务

### 负责文件：`local_game.py`

> **定位**：本地同屏双人回合对战。导入C逻辑，管理回合，提供HTTP接口给前端。  
> **依赖**：强依赖 `core/game_logic.py`（导入 `GameLogic` 类）  
> **被对接**：B 的前端通过 HTTP JSON 与本文件通信

### 4.1 技术方案

```python
# 使用 Python 内置 http.server 或 Flask
# 推荐 Flask（pip install flask）—— 更简洁，适合快速开发
# 若不想额外安装，可用 http.server + 手动解析 JSON
```

### 4.2 类与方法清单

```python
from core.game_logic import GameLogic
# 若用Flask: from flask import Flask, request, jsonify

class LocalGame:
    """
    本地双人对战服务
    职责：回合控制 / HTTP通信 / 调用C的逻辑
    禁止：重复实现飞机摆放、攻击判定等（必须用C的方法）
    """

    # ========== 属性 ==========
    logic: GameLogic       # C层核心逻辑实例
    current_turn: int      # 当前回合玩家 (0 或 1)
    game_over: bool        # 游戏是否结束
    winner: int | None     # 胜者编号

    # ========== 方法 ==========

    def __init__(self) -> None:
        """
        1. 创建 GameLogic 实例 → 自动初始化棋盘+摆放飞机
        2. 设置 current_turn = 0 (玩家1先手)
        3. game_over = False, winner = None
        """

    def handle_attack(self, player_id: int, row: int, col: int) -> dict:
        """
        【核心方法】处理一次攻击请求
        参数:
            player_id: 攻击方 0 或 1
            row, col:  攻击坐标
        返回:
            {
                "result":      0|1|2,        # 攻击结果
                "message":     "未命中"|"命中机身"|"命中机头！飞机击毁！",
                "current_turn": 0|1,         # 下一回合谁出手
                "game_over":   true|false,
                "winner":      null|0|1,
                "your_board":  [[...]],       # 攻击方自己的棋盘（显示飞机位置）
                "enemy_board": [[...]]        # 被攻击方的棋盘（只显示已攻击结果）
            }
        逻辑:
            1. 校验: 不是该玩家回合 → 返回错误
            2. 校验: 游戏未结束
            3. 调用 logic.attack(1-player_id, row, col) 攻击对方棋盘
            4. 调用 logic.is_player_defeated(1-player_id) 检查对方是否全灭
            5. 切换回合: current_turn = 1 - player_id
            6. 组装返回字典
        """

    def get_game_state(self) -> dict:
        """
        返回当前游戏全局状态（供前端刷新/初始化）
        返回结构同上（不含攻击结果）
        """

    def restart(self) -> None:
        """
        重置游戏:
        1. 重新实例化 GameLogic
        2. current_turn = 0
        3. game_over = False
        """
```

### 4.3 HTTP 路由设计

```python
# ===== 若使用 Flask =====

app = Flask(__name__)
game = LocalGame()

@app.route('/attack', methods=['POST'])
def api_attack():
    """
    接收前端攻击请求
    请求体: { "player": 0|1, "row": 0-9, "col": 0-9 }
    响应体: handle_attack() 返回的字典
    """
    data = request.get_json()
    result = game.handle_attack(data['player'], data['row'], data['col'])
    return jsonify(result)

@app.route('/state', methods=['GET'])
def api_state():
    """返回当前游戏状态"""
    return jsonify(game.get_game_state())

@app.route('/restart', methods=['POST'])
def api_restart():
    """重置游戏"""
    game.restart()
    return jsonify({"status": "ok"})

if __name__ == '__main__':
    app.run(host='0.0.0.0', port=5000, debug=True)


# ===== 若使用内置 http.server（备选方案）=====
# from http.server import HTTPServer, BaseHTTPRequestHandler
# 在 do_POST 中解析路径和JSON，调用 game.handle_attack()
# 用 json.dumps() 写回响应
```

> **要实现的具体内容：**
> - `LocalGame` 类（4个方法：`__init__` / `handle_attack` / `get_game_state` / `restart`）
> - HTTP 路由：`POST /attack` + `GET /state` + `POST /restart`
> - 前后端数据格式严格按上述JSON结构
> - 与B联调：确保B的 `sendAttackToLocal()` 能正确收到响应

---

## 五、WebSocket联机对战服务

### 负责文件：`server.py`

> **定位**：异地双人联网对战。导入C逻辑，WebSocket通信，房间管理。  
> **依赖**：强依赖 `core/game_logic.py`；参考回合逻辑改造  
> **被对接**：前端通过 WebSocket 直连本服务

### 5.1 技术栈

```bash
pip install websockets   # WebSocket 服务端库
# 无需额外安装 — Python 内置 asyncio 即可
```

### 5.2 类与方法清单

```python
import asyncio
import json
import uuid
from core.game_logic import GameLogic

class Room:
    """
    游戏房间
    一个房间 = 2个玩家 + 1个GameLogic实例
    """

    # ========== 属性 ==========
    room_id: str           # 房间号 (uuid生成)
    players: list          # [ws1, ws2] 两个WebSocket连接
    logic: GameLogic       # 核心逻辑实例
    current_turn: int      # 当前回合玩家 (0或1)
    game_started: bool     # 是否已开始

    # ========== 方法 ==========

    def __init__(self, room_id: str):
        """初始化房间 — 创建GameLogic实例, game_started=False"""

    def add_player(self, websocket) -> int:
        """
        加入房间，返回玩家编号 0 或 1
        满员(2人)后设 game_started=True
        """

    def is_full(self) -> bool:
        """房间是否满员"""

    async def handle_attack(self, player_id: int, row: int, col: int) -> dict:
        """
        处理攻击 — 调用 logic.attack() + logic.is_player_defeated()
        返回结果字典，与D层的 handle_attack 结构一致
        """

    async def broadcast(self, message: dict):
        """向房间内两人广播同一消息"""


class Server:
    """
    WebSocket 对战服务器
    """

    # ========== 属性 ==========
    rooms: dict            # {room_id: Room} 所有房间
    pending_connections: dict  # {ws: ...} 等待中的连接

    # ========== 方法 ==========

    def __init__(self):
        """初始化 rooms = {}"""

    def create_room(self) -> str:
        """创建新房间，返回 room_id"""

    def get_room(self, room_id: str) -> Room | None:
        """根据ID查找房间"""

    async def handler(self, websocket, path):
        """
        【核心】每个WebSocket连接的处理入口
        逻辑:
            1. 接收消息循环
            2. 按 type 字段分发:
               "create_room" → 创建房间 → 回复 room_created
               "join_room"   → 加入房间 → 回复 room_joined
               "attack"      → 调用 Room.handle_attack() → broadcast 结果
            3. 连接断开 → 清理房间, 通知另一方
        """

    async def start(self, host='0.0.0.0', port=8765):
        """启动WebSocket服务"""
```

### 5.3 WebSocket 消息协议（与前端页面约定）

```python
# ===== 前端 → 服务端 =====
# 1. 创建房间
{ "type": "create_room" }

# 2. 加入房间
{ "type": "join_room", "room_id": "abc123" }

# 3. 发起攻击
{ "type": "attack", "room_id": "abc123", "row": 3, "col": 5 }

# 4. 重新开始
{ "type": "restart", "room_id": "abc123" }


# ===== 服务端 → 前端 =====
# 1. 房间创建成功
{ "type": "room_created", "room_id": "abc123", "player": 0 }

# 2. 加入房间成功
{ "type": "room_joined", "room_id": "abc123", "player": 1 }

# 3. 游戏开始（2人到齐）
{ "type": "game_start", "your_board": [[...]], "enemy_board_hidden": [[...]], "current_turn": 0 }

# 4. 攻击结果
{
    "type": "attack_result",
    "result": 0|1|2,
    "message": "未命中",
    "current_turn": 0|1,
    "game_over": false,
    "winner": null,
    "your_board": [[...]],
    "enemy_board_display": [[...]]   # 只暴露已攻击格
}

# 5. 对方掉线
{ "type": "opponent_disconnected", "message": "对方已断开连接" }

# 6. 错误
{ "type": "error", "message": "房间不存在" }
```

### 5.4 异常处理

```python
# 需要捕获并处理的异常场景：
# 1. websockets.exceptions.ConnectionClosed → 玩家断开
# 2. 房间不存在 → 返回 error 消息
# 3. 房间已满 → 返回 error 消息
# 4. 不是你的回合 → 返回 error 消息
# 5. 重复攻击 → 返回 error 消息
# 6. 游戏已结束 → 返回 error 消息
# 7. JSON 解析失败 → 返回 error 消息
```

> **要实现的具体内容：**
> - `Room` 类（5个方法：`__init__` / `add_player` / `is_full` / `handle_attack` / `broadcast`）
> - `Server` 类（4个方法：`__init__` / `create_room` / `get_room` / `handler` / `start`）
> - 7种 WebSocket 消息 type 的处理
> - 6种异常场景的捕获
> - 与前端联调：确保前端的 `sendAttackToServer()` 能正确通信

---

## 六、管殷正：规范、文档、整合、答辩

### 6.1 负责文件清单

| 文件 | 内容 | 面向谁 |
|------|------|--------|
| `README.md` | 项目说明、安装运行步骤、技术栈介绍 | 老师/评委 |
| `docs/游戏规则文档.md` | 10×10规则、1513坐标、判定、胜负、对战流程 | 全员参考 |
| `docs/接口说明文档.md` | C/D/E所有接口定义、JSON格式、调用示例 | B/D/E联调 |
| `docs/使用手册.md` | 本地模式怎么玩、联机模式怎么连 | 用户/评委 |
| `docs/测试报告.md` | 汇总C/B/D/E各模块测试结果 | 老师/评委 |
| `答辩材料/答辩PPT.pptx` | 项目介绍、技术栈、分工、演示流程、亮点 | 答辩评委 |
| `答辩材料/演示流程说明.md` | 按步骤编排的演示脚本 | 全员彩排 |

### 6.2 各文档内容结构

```
README.md:
  1. 项目名称 + 一句话简介
  2. 技术栈
  3. 项目结构目录树
  4. 环境要求 (Python 3.x, 依赖库)
  5. 安装与运行
     - 本地模式: python local_game.py → 打开 index.html
     - 联机模式: python server.py → 打开两个浏览器窗口
  6. 游戏规则简述
  7. 成员分工表
  8. 截图预览 (加分项)

docs/游戏规则文档.md:
  1. 棋盘规格
  2. 飞机形状 (1513规则 + 四种朝向图示)
  3. 飞机摆放规则 (不出界、不重叠)
  4. 攻击判定规则 (0/1/2)
  5. 机头击毁规则 (整架飞机标记摧毁)
  6. 胜负判定
  7. 回合交替规则

docs/接口说明文档.md:
  1. C层接口 — GameLogic 全部方法签名+参数+返回值+示例
  2. D层接口 — HTTP路由、请求/响应JSON格式、示例
  3. E层接口 — WebSocket消息协议、所有type枚举、示例
  4. 错误码约定

docs/使用手册.md:
  1. 启动本地对战
  2. 启动联机对战（创建房间 → 分享房间号 → 加入）
  3. 操作说明（点击敌方棋盘 → 查看结果 → 等待回合）
  4. 界面说明（各部分功能标注）
  5. 常见问题 FAQ

docs/测试报告.md:
  1. C层测试结果（10项自测通过情况）
  2. B前端测试（格子渲染、点击响应、样式切换）
  3. D本地联调测试（完整一局无报错）
  4. E联机联调测试（双窗口对战无报错）
  5. 已知问题 + 解决方案

答辩材料/答辩PPT.pptx:
  1. 封面 (项目名+组名)
  2. 项目背景与目标
  3. 技术架构 (文件依赖图)
  4. 核心功能展示 (截图)
  5. 技术亮点 (双模式、WebSocket、模块解耦)
  6. 成员分工
  7. 开发过程 (时间线)
  8. 遇到的问题+解决方案
  9. 总结与展望
  10. 致谢

答辩材料/演示流程说明.md:
  Step 1: 启动本地模式，展示双棋盘 + 对战一局
  Step 2: 展示机头命中、飞机击毁的特殊效果
  Step 3: 启动联机模式，两个浏览器窗口对战
  Step 4: 展示房间创建+加入流程
  Step 5: 展示断线重连（如有）
```

## 七、全局数据流转图

### 7.1 本地对战模式

```
[用户] 点击敌方棋盘格子 (row=3, col=5)
   │
   ▼
【 - main.js】
   onCellClick() → sendAttackToLocal(3, 5)
   │
   │  fetch POST http://localhost:5000/attack
   │  Body: {"player": 0, "row": 3, "col": 5}
   ▼
【 - local_game.py】
   LocalGame.handle_attack(player=0, row=3, col=5)
   │
   │  调用核心游戏逻辑
   ▼
【 - core/game_logic.py】
   GameLogic.attack(player_id=1, row=3, col=5)
   读取 board_p1[3][5]
   → 假设值为1(机身) → 加入 attacked_p1 → 返回 HIT(1)
   │
   │  返回结果
   ▼
【 - local_game.py】
   收到 result=1
   调用 logic.is_player_defeated(1) → False
   更新 current_turn = 1
   组装响应 JSON
   │
   │  HTTP Response (JSON)
   ▼
【 - main.js】
   handleAttackResult(data)
   updateCellStyle("board-enemy", 3, 5, 1)  → 格子变红
   更新文字提示: "命中机身！轮到玩家2"
```

### 7.2 联机对战模式

```
[玩家1浏览器]                       [玩家2浏览器]
【 - main.js】                     【 - main.js】
   │                                   │
   │ ws.send({attack, room_id})        │
   ▼                                   │
【 - server.py】                       │
   Server.handler()                     │
   接收玩家1的攻击消息                     │
   │                                   │
   │ 查找房间 → Room.handle_attack()     │
   ▼                                   │
【 - game_logic.py】                   │
   attack() → 返回结果                   │
   │                                   │
   ▼                                   │
【 - server.py】                       │
   Room.broadcast()                     │
   │                                   │
   ├──→ ws.send(前端1, attack_result) ──→ 【前端1】渲染结果
   │                                   │
   └──→ ws.send(前端2, attack_result) ──→ 【前端2】渲染结果
                                        │
                                  两人同时看到更新
```

---

## 八、接口契约（所有项目文件必须遵守）

### 8.1 核心逻辑提供给两个对战的方法（不可更改签名）

```python
# 以下 4 个公开方法一经确认，不得擅自修改参数和返回值类型
logic = GameLogic()                           # 无参数
logic.place_planes(player_id: int) -> list     # 1参，返回二维list
logic.attack(player_id: int, row: int, col: int) -> int  # 3参，返回int
logic.is_player_defeated(player_id: int) -> bool  # 1参，返回bool
logic.get_board_state(player_id: int) -> list    # 1参，返回二维list
```

### 8.2 HTTP 接口（不可更改格式）

```
POST /attack  → 请求 {"player": int, "row": int, "col": int}
                响应 {"result": int, "message": str, "current_turn": int, ...}
GET  /state   → 响应 完整游戏状态
POST /restart → 响应 {"status": "ok"}
```

### 8.3 WebSocket 协议（不可更改字段名）

```
所有消息必须包含 "type" 字段
攻击消息必须包含 "room_id", "row", "col" 字段
结果消息必须包含 "result", "message", "current_turn", "game_over" 字段
```

---
